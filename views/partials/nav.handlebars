<nav class="sticky w-full z-10 top-0 bg-lightblue">
    <div class="flex border-b mx-auto content-center items-center justify-between mt-0 py-3">
        <a class="flex-1 pl-4 text-xl font-semibold" href="/">Chattr</a>
        <ul class="flex flex-row">
            <li class="mr-4">
                {{#if user}}
                <a class="" href="/log-out">Log out</a>
                {{else}}
                <a class="shadow-hard border-2 rounded px-2 py-1 mx-1" href="/sign-up">Sign Up</a>
                <a class="shadow-hard border-2 rounded px-2 py-1 mx-1" href="/log-in">Log in</a>
                {{/if}}
            </li>
        </ul>
    </div>
</nav>